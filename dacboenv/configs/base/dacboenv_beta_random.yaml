# @package _global_
defaults:
  - dacboenv

optimizer_id: DACBOEnv-SMAC3-beta-random

optimizer:
  smac_cfg:
    smac_kwargs:
      acquisition_function: 
        _target_: dacboenv.utils.confidence_bound.UCB
        update_beta: false