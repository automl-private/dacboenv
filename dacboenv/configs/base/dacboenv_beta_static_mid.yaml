# @package _global_
defaults:
- dacboenv
optimizer:
  policy_class: dacboenv.env.policy.StaticParameterPolicy
  policy_kwargs:
    par_val: -1
  smac_cfg:
    smac_kwargs:
      acquisition_function:
        _target_: dacboenv.utils.confidence_bound.UCB
        update_beta: false
optimizer_id: DACBOEnv-SMAC3-beta-static-mid
